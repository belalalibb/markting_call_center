# OPS 5.5 Independent Audit — 2026-09-23 (baseline 611c706 / cp/CP-0012)

Operator approved the full remediation program (P0→P4) on 2026-09-23. This file is the durable copy of the findings
register and plan; the full narrative report was delivered in-session. Probes lived in /tmp/audit (not tracked).

## Decision
Needs targeted refactor. Protect: CP-0012 keepalive, OpenAI GA wire shape, direct audio streaming, Egyptian
instructions + governance text, rules-authoritative decisions, contracts-only Core, memory-only credentials,
replay/sim/eval, recovery protocol, barge-in cancel timing (~1 ms t1→t2, ~55 ms t1→t3).

## Findings (status at audit time)
| ID | Sev | Finding | Root cause (file) |
|---|---|---|---|
| F-01 | BLOCKER | Voice: 54 audio appends reach OpenAI before any session.update → provider defaults (voice alloy, default persona, server_vad auto-responses, no tools) for the whole first utterance; at first EOT OpenAI returns `input_audio_buffer_commit_empty` + `conversation_already_has_active_response`; activity BLOCKED; outcome technical_failure; default persona invented a price | `SessionLifecycle.start()` composes but never pushes session config (core/session.py) |
| F-02 | CRITICAL | SPEAKING→LISTENING on provider `response.done`, not on client playout end; provider streams faster than realtime (25 s audio in ~4.9 s) → speech during the audible tail becomes a new turn, no stop_playout, replies queue | core/session.py RESPONSE_DONE + handle_audio `speaking` flag |
| F-03 | HIGH | Two VADs on turn 1 (provider default + local) | consequence of F-01 |
| F-04 | HIGH | `response_started` while INTERRUPTED is illegal-logged but audio still flows; UI state wrong | core/session.py RESPONSE_STARTED |
| F-05 | HIGH | Every provider error non-retryable → BLOCKED sticky → technical_failure; error message dropped | core/session.py ERROR, openai_realtime.py |
| F-06 | HIGH | Unheard assistant text never truncated at provider (`conversation.item.truncate` absent) | contracts/provider.py S2SSession port, adapter |
| F-07 | CRITICAL (deploy) | No auth on any route; CORS `*`; key hijack/delete via /api/admin/test-key proven | runtime/app.py |
| F-08 | HIGH | /api/events and /api/sessions cross-tenant | runtime/app.py |
| F-09 | MEDIUM | Composition says silero; energy fallback actually runs (no model, no onnxruntime) — not surfaced | adapters/turn/silero.py, UI |
| F-10 | MEDIUM | `user.speech_committed` emitted for both turn commit and transcript | core/session.py INPUT_TRANSCRIPT |
| F-11 | HIGH | Browser/WS smokes pass while provider errors + technical_failure occur | scripts/browser_voice_smoke.py, tests |
| F-12 | MEDIUM | event_log / sessions unbounded | runtime/store.py |
| F-13 | HIGH | Operator never sees transcripts ("did it hear me?"); silent tool responses show "(speaking…)" | core/session.py, web/src/console.ts |
| F-14 | MEDIUM | UX: default mock, mock shows "no credential", non-actionable errors, misleading mic error, mobile nav overlap, Admin lists providers without adapters | web/src/* |
| F-15 | LOW | Docs overstate voice verification (QV-ACC-022) | QEVION_WORK_STATE.md, traceability |

## Plan
| P | Checkpoint | Items |
|---|---|---|
| P0 | CP-0013 | F-01, F-05, F-07 (admin token + CORS allow-list), F-11 |
| P1 | CP-0014 | F-02, F-04, F-06, F-13, F-03 confirm |
| P2 | CP-0015 | transcription model option, F-09 honesty, real-speech corpus harness, latency table |
| P3 | (CP-0016) | F-08, F-12, F-10 |
| P4 | (CP-0017) | Normal/Advanced UI, mobile nav, first-run banner, actionable errors, F-14, F-15 |

## Acceptance criteria
- Voice live (OpenAI, synthetic speech clips, 3 turns): first provider msg `session.update`; 0 responses before first
  EOT; 0 provider.error; 0 illegal_dialog_transition; session.updated voice = marin; outcome ≠ technical_failure.
- Barge-in 1 s after response.done with audio still queued → stop_playout ≤150 ms + truncate sent.
- /api/admin/* without token → 401 when a token is configured; foreign-origin CORS not allowed.
- CI ≥273 tests pass; keepalive soak no 1011; t1→t3 ≤300 ms; ready ≤1 s.
