# CP-0016 — OPS 5.5 P3: F-08 tenant-scoped read tokens + filtered sessions/events, F-12 bounded event ring + session pruning

| Field | Value |
|---|---|
| Checkpoint ID | CP-0016 |
| Timestamp (UTC) | 2026-09-24T00:09:18Z |
| Phase | P3-rem |
| Git SHA (state described) | 6fae33942a1d9ad5c5e73369b4b62ec25f73cee6 |
| Status | VERIFIED |

## Completed work (OPS 5.5 audit P3)
- **F-08:** `QEVION_TENANT_TOKENS="tenant:token,…"` read-only tenant tokens (GET `/api/sessions*`, `/api/events` only; anything else 403). Handlers filter by the token's tenant; `?tenant=` cannot widen scope; a foreign session_id returns 404. Operator token sees all (optional `?tenant=` filter). Sessions carry `tenant_id`. `/api/events?session_id=` added (health harness uses it).
- **F-12:** event log is a ring buffer (`QEVION_MAX_EVENTS`, default 50000) with absolute `since` offsets; per-session events capped (`QEVION_MAX_SESSION_EVENTS` 5000); finished sessions pruned past `QEVION_MAX_SESSIONS` (200), live sessions never evicted. Limits are shown in `/api/health`.
- F-10 is fully done (user.transcript since CP-0014).

## Verified work (command → result)
- `scripts/ci_local.sh` → **CI_LOCAL: PASS — 297 tests** (`tests/test_p3_tenant_bounds.py` 3 new).
- Live OpenAI regression `evidence/P3/CP-0016/trace_regression.json` → 8/8 health checks.

## Unverified work
- Per-tenant *write* roles (tenant tokens are read-only by design); no persistent store (in-memory only).

## Files changed since cp/CP-0015
```
 QEVION_WORK_STATE.md                      |    7 +-
 evidence/P3/CP-0016/trace_regression.json | 1365 +++++++++++++++++++++++++++++
 evidence/ci/junit.xml                     |    2 +-
 qevion/main.py                            |    2 +-
 qevion/runtime/app.py                     |   35 +-
 qevion/runtime/auth.py                    |   54 +-
 qevion/runtime/store.py                   |   47 +-
 recovery/snapshots/CP-0015.manifest.json  |  337 +++++++
 scripts/session_health.py                 |    2 +-
 tests/test_p3_tenant_bounds.py            |   75 ++
 10 files changed, 1909 insertions(+), 17 deletions(-)
```

## Tests / evidence
- evidence/P3/CP-0016/*, evidence/ci/junit.xml

## Known failures (classified)
- none

## Known risks
- Pruned telemetry is gone (no durable sink yet).

## Exact next step
- P4 (CP-0017): Normal/Advanced UI, mobile nav overlap, first-run banner, actionable errors (no key → Admin link; mic denied hint), Admin provider list restricted to adapters (drop deepgram/elevenlabs), default composition/mock clarity, F-14, F-15 docs correction (QV-ACC-022).
