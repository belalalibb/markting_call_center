# CP-0010 — TypeSafe Jev live decision role (rules+typesafe), WS E2E + smoke evidence

| Field | Value |
|---|---|
| Checkpoint ID | CP-0010 |
| Timestamp (UTC) | 2026-09-23T13:03:38Z |
| Phase | P6+ |
| Git SHA (state described) | 29cf55614c945c9e6582562b0a74166293ac010e |
| Status | VERIFIED |

## Completed work
- **TypeSafe Jev decision adapter (post-P6, operator key supplied → A5 lifted for the decision role only):** `qevion/adapters/decision/typesafe.py` — `TypeSafeDecisionAdapter` (Jev `POST /v1/systemone` closed-choice questions; supports `interpret_confirmation` + `classify_intent` only; every other DecisionKind refused = UNKNOWN; https-only transport guard; confidence floors 0.6 / 0.75; UNKNOWN on no credential / network error / unmapped choice / low confidence; evidence_refs carry model + probabilities, never customer text) and `LayeredDecisionAdapter` (`rules+typesafe`: rules authoritative, provider consulted only when rules return UNKNOWN — ADR-0004 preserved). Capabilities declared honestly (ar-EG PARTIAL).
- **Wiring:** composition `config/compositions/comp_mock_s2s_typesafe_v1.yaml`; `runtime/store.py` resolves the `typesafe` credential per session (env `TYPESAFE_API_KEY` → admin store → Admin ephemeral test-key) and exposes `decision_credential_source`; named mock script `?script=callback` (opening → `schedule_callback` tool step → confirmation gate) for live decision smokes; `decision.made` + tool confirmation/execution events mirrored to the client (operator telemetry only, no text/secrets); `/api/compositions` shows `decision_adapter` + `decision_credential_source`; `admin/credentials._ENV_KEYS["typesafe"]`.
- **Tests/scripts:** `tests/test_adapter_typesafe_decision.py` (8 offline tests w/ FakeHttp), `scripts/typesafe_live_smoke.py` (raw adapter + layered + core ScenarioRunner session), `scripts/typesafe_live_ws.py` (running-server WS E2E, 3 phrases). `.env.example` gains `TYPESAFE_API_KEY=`.
- **Registry/docs:** QV-ACC-022 → `partial` (decision role live-verified; S2S role still A5-blocked). Work state + incident #16 (resets #22–#29).

## Verified work (command → result)
- `scripts/ci_local.sh` → **CI_LOCAL: PASS** — 268 tests, 0 failures (ruff, format, mypy strict, lint-imports 5/5 KEPT, schema drift, verify.sh secret scan, license gate). `evidence/ci/junit.xml`.
- `TYPESAFE_API_KEY=… scripts/typesafe_live_smoke.py evidence/live/typesafe_live_smoke.json` → exit 0: 18 cases, 16 matches, 2 abstentions (Arabizi negative 0.21–0.25; ar-EG opt-out intent 0.42 < 0.75 floor), **0 contradictions**; layered pass: rules decided the lexicon hits, provider only on UNKNOWN; core session `callback_confirmed_spoken_ar` passed → `callback_requested` with an LLM_VALIDATED confirmation; 23 provider calls, latency p50 202 ms / p95 292 ms / max 337 ms; key stored as fingerprint only.
- `TYPESAFE_API_KEY=… scripts/typesafe_live_ws.py evidence/live/typesafe_live_ws_e2e.json` against `uvicorn qevion.main:app` with the key injected via `POST /api/admin/test-key` (credential_source `ephemeral_ui`) → **3/3 passed**: "خلاص يا عم اتفقنا كده" → `decision.made {LLM_VALIDATED, true, 1.0}` → `tool.confirmation_granted` → `tool.execution_completed schedule_callback`; "لا يا عم بلاش سيبها" → `{RULE, false, 0.8}` → `tool.confirmation_denied` (no provider call); "هو ده هيكون امتى بالظبط؟" → `{UNKNOWN, null, 0.0}` → neither granted nor denied.
- `grep -rl apikey_ evidence/ scripts/` → no secret in tree.

## Unverified work
- S2S real provider (OpenAI Realtime / Gemini Live) — still no key (A5). Web console does not yet display `decision_credential_source` (optional backlog).

## Files changed since cp/CP-0009
```
 .env.example                                       |   1 +
 QEVION_WORK_STATE.md                               |  18 +-
 config/compositions/comp_mock_s2s_typesafe_v1.yaml |  11 +
 docs/registry/traceability.yaml                    |   2 +-
 evidence/ci/junit.xml                              |   2 +-
 evidence/ci/licenses.json                          |   2 +-
 evidence/live/typesafe_live_smoke.json             | 357 +++++++++++++++++++++
 evidence/live/typesafe_live_ws_e2e.json            |  89 +++++
 qevion/adapters/decision/typesafe.py               | 206 ++++++++++++
 qevion/admin/credentials.py                        |   1 +
 qevion/runtime/app.py                              |  22 +-
 qevion/runtime/store.py                            |  46 ++-
 qevion/simulation/runner.py                        |   3 +-
 recovery/snapshots/CP-0009.manifest.json           | 276 ++++++++++++++++
 scripts/typesafe_live_smoke.py                     | 210 ++++++++++++
 scripts/typesafe_live_ws.py                        | 201 ++++++++++++
 tests/test_adapter_typesafe_decision.py            | 111 +++++++
 17 files changed, 1541 insertions(+), 17 deletions(-)
```

## Tests / evidence
- (paths)

## Known failures (classified)
- none

## Known risks
- (fill)

## Exact next step
- (one action)
