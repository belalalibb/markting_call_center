# CP-0008 — Simulation harness + Activation gates + Outbound seam

| Field | Value |
|---|---|
| Checkpoint ID | CP-0008 |
| Timestamp (UTC) | 2026-09-23T04:33:50Z |
| Phase | P5 |
| Git SHA (state described) | 270c56c33b59b8fb1cec4a4f3ec01400e4d0a15e |
| Status | VERIFIED |

## Completed work
- `qevion/contracts/simulation.py` — `qevion.simulation_case.v1` + `qevion.simulation_report.v1` (PersonaKind ×10 required set, Injection ×9, GraderId ×10 with safety/completion split, CustomerTurn, ScenarioCase, GraderResult, ScenarioResult, ActivationThresholds 100 %/90 %, SimulationFinding → Blueprint paths, `session_kind=simulation`); registered (42 schemas); P0 placeholder in `telemetry.py` retired
- `qevion/simulation/customer.py` — `SimulatedCustomerTransport`: script-driven `TransportSession` (says/interrupts/confirms/denies/hangs up; answers confirmation requests; stamps playout); the runtime does not know it is simulated (QV-SIM-001)
- `qevion/simulation/runner.py` — `ScenarioRunner` builds a real `core.session.Session` per case (mock provider scripted *from the case*, memory backends, rules decision port, deterministic 5 ms clock); `grade()` = 10 evidence-based graders (events/InteractionRecord/FieldStore/claim log); `build_report()` with per-grader findings, required-persona coverage, adversarial requirement (happy-path-only = BLOCK, QV-SIM-005); `blueprint_fingerprint`
- `qevion/simulation/cases.py` — `default_cases(bp)`: 10 required personas + injected conditions (tool failure, incomplete info, human request, callback+confirm, opt-out) derived from Blueprint data only (field names/types, permissions, outcome schema)
- `qevion/runtime/store.py` — `run_simulation()` (report drives `READY_FOR_ACTIVATION` / `SIMULATION_FAILED`), `activation_gates()`, `activate()` (refusal names unmet gates), `preflight_ref`; **outbound seam**: `OutboundAttempt` (hooks → dial → session; refusals recorded without dial), `contact_state`, `check_contact`, `outbound_attempt()` via `SimulatedTelephonyAdapter`, `LiveSession.outbound_attempt_id`
- `qevion/control/contact_policy.py` — QV-OUT-DIR-002 hooks: consent (unknown = refused), opt-out, suppression (flag or `suppression_ref` tag), attempt limit, contact window with tz + overnight + unparseable → conservative refusal; every failing hook reported
- `qevion/runtime/app.py` — `POST /simulate`, `GET /simulation`, `GET /gates`, `POST /activate` (409 with `unmet`), contacts `GET/PUT /api/contacts/{ref}`, `GET /contact-check/{ref}`, `GET /api/outbound/attempts`, WS `/ws/outbound/{key}/{contact}` (4403 refused / 4480 not answered / 4400 inbound-only); shared `_pump`; summary carries `simulation` + `unapproved_decisions`
- `qevion/contracts/event.py` — `outbound.contact_checked|dial_started|dial_result`
- `scripts/p5_evidence.py` — reproducible ACC-020/021 evidence from the real store

## Verified work (command → result)
- `scripts/ci_local.sh` → `CI_LOCAL: PASS` (ruff, ruff format, mypy strict, lint-imports 5 kept / 0 broken, schema drift 42 ok, pytest **243 passed / 0 failed**, verify.sh PASS, license gate 0 denied) — `evidence/P5/CP-0008/junit.xml`
- `pytest tests/test_simulation_runner.py` → 7 passed (happy path all graders; callback confirm/deny gate; handoff on Activity B + permission gap surfaced on C; seeded violations fail safety graders + BLOCK findings; persona-coverage/adversarial requirements; budget guard; tool-failure injection graded not raised)
- `pytest tests/test_control_contact_policy.py` → 8 passed · `tests/test_runtime_api.py` → 27 passed (+8: activation refused pre-simulation; simulate→READY_FOR_ACTIVATION→ACTIVE; seeded → SIMULATION_FAILED + 409; outbound refused w/o consent recorded with no dial and no session; opt-out/suppression; answered → same-Core session with `outbound_attempt_id`, attempts counted, limit refused; no-answer 4480 still counts a dial; inbound activity 4400)
- `scripts/p5_evidence.py evidence/P5/CP-0008` → `{"acc020": true, "acc021": true}` — **QV-ACC-020**: clean Activity C: 14 cases / 10 persona kinds / 10 adversarial, safety 1.0, completion 1.0 → `READY_FOR_ACTIVATION` → `ACTIVE` with `preflight_result_ref` + `simulation_report_ref`; seeded copy (`schedule_callback.confirmation=none`, `prohibited_claims=[]`, handoff expected but unpermitted): 15 cases, safety 0.9889 → `SIMULATION_FAILED`, BLOCK finding `handoff_correct → [policies.escalation, tools.permissions]`, activation refused (`unmet=[simulation_passed, version_frozen]`) · **QV-ACC-021**: `max_write_calls=1` → 7 tool requests, 1 executed, 6 × `budget.exceeded`, session CLOSED cleanly
- `git ls-remote origin main` == local HEAD after every commit

## Unverified work
- none

## Files changed since cp/CP-0007
```
 QEVION_WORK_STATE.md                               |   24 +-
 docs/registry/traceability.yaml                    |    4 +-
 evidence/P5/CP-0008/acc020_simulation_gate.json    |   66 +
 evidence/P5/CP-0008/acc021_budget_guard.json       |   30 +
 evidence/P5/CP-0008/index.json                     |   62 +
 evidence/P5/CP-0008/junit.xml                      |    1 +
 evidence/P5/CP-0008/licenses.json                  |  252 +++
 evidence/P5/CP-0008/simulation_report_clean.json   | 1691 ++++++++++++++++++++
 evidence/ci/junit.xml                              |    2 +-
 qevion/contracts/event.py                          |    3 +
 qevion/contracts/registry.py                       |    4 +-
 qevion/contracts/schemas/index.json                |    1 +
 .../schemas/simulation_case_v1.schema.json         |  274 ++++
 .../schemas/simulation_report_v1.schema.json       |  388 +++--
 qevion/contracts/simulation.py                     |  181 +++
 qevion/contracts/telemetry.py                      |   27 +-
 qevion/control/contact_policy.py                   |   91 ++
 qevion/runtime/app.py                              |  195 ++-
 qevion/runtime/store.py                            |  132 +-
 qevion/simulation/__init__.py                      |    2 +-
 qevion/simulation/cases.py                         |  280 ++++
 qevion/simulation/customer.py                      |  122 ++
 qevion/simulation/runner.py                        |  476 ++++++
 recovery/snapshots/CP-0007.manifest.json           |  236 +++
 scripts/e2e_voice_probe.py                         |  124 +-
 scripts/p5_evidence.py                             |  162 ++
 tests/test_contracts_roundtrip.py                  |   14 +-
 tests/test_control_contact_policy.py               |   72 +
 tests/test_runtime_api.py                          |  200 +++
 tests/test_simulation_runner.py                    |  252 +++
 30 files changed, 5174 insertions(+), 194 deletions(-)
```

## Tests / evidence
- `evidence/P5/CP-0008/{junit.xml,licenses.json,acc020_simulation_gate.json,acc021_budget_guard.json,simulation_report_clean.json,index.json}`
- `docs/registry/traceability.yaml`: QV-ACC-020 accepted, QV-ACC-021 accepted

## Known failures (classified)
- none

## Known risks
- Graders are deterministic and evidence-based but the *customer* is fully scripted (no bounded-LLM customer yet, QV-SIM-001 optional part); model-graded signals absent — reported as `model_graded=False` everywhere.
- `NO_INVENTED_CLAIMS` relies on the ClaimGovernor log; the mock provider never asks the governor, so on mocks this grader mostly proves the *policy* shape (seeded test drives the governor directly). Real-provider runs will exercise it for real.
- Outbound uses `SimulatedTelephonyAdapter`; contact state is in-memory (`RuntimeStore.contacts`) — a CRM/DNC adapter is a P6+/product concern (hooks are the required seam, jurisdiction logic DEFERRED per spec).
- Contact-window evaluation depends on system tzdata (`zoneinfo`); unparseable windows refuse conservatively.
- Sandbox resets #16–#20 this phase; recovered every time from committed state (index/CP records redone after verification).

## Exact next step
- `tag_checkpoint.sh CP-0008` → `snapshot.sh CP-0008` → update `QEVION_WORK_STATE.md` (P5 COMPLETE → P6) → P6 step 1: `qevion/eval/` evaluation harness (copilot eval suite QV-EVAL-004 + runtime graders reuse) and `qevion/replay/` deterministic replay (QV-ACC-019: recorded event seq → same events/outcome), then Evidence pack + verified Preview URL → CP-0009.
