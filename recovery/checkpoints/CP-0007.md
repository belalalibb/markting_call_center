# CP-0007 — Voice runtime: browser voice console, OpenAI Realtime + Silero/SmartTurn adapters, compositions, §31 t0..t4 E2E

| Field | Value |
|---|---|
| Checkpoint ID | CP-0007 |
| Timestamp (UTC) | 2026-09-23T03:31:15Z |
| Phase | P4 |
| Git SHA (state described) | b58e391e839dbcbddd70412f174743e98677e09c |
| Status | VERIFIED |

## Completed work
- `qevion/adapters/providers/openai_realtime.py` — S2S adapter over WebSocket with injectable `SocketFactory` (offline-testable); protocol mapping in module docstring (`session.update` instructions/tools, `input_audio_buffer.append/commit`, `response.cancel` on barge-in + local audio queue drain, function calls → ToolCallRequest); `open()` raises PermissionError without credential; capabilities: barge_in/tool_calls SUPPORTED, language UNVERIFIED
- `qevion/adapters/turn/silero.py` — Silero VAD behind `TurnDetector` (optional ONNX `models/silero_vad.onnx` + `[vad]` extra); honest fallback `silero_fallback_energy` with `feature:vad:silero` UNVERIFIED; 24k→16k decimation, 512-sample windows
- `qevion/adapters/turn/smart_turn.py` — `SmartTurnDetector` wraps energy detector; END_OF_TURN_CANDIDATE scored by `EndOfTurnScorer` (≥0.75 early commit, ≤0.3 extend min_silence once by 400 ms); capabilities PARTIAL unless model-backed
- `qevion/runtime/store.py` — composition registry (`config/compositions/*.yaml`), adapter registries by name (s2s: mock/openai_realtime; turn: energy/mock/silero/smart_turn; decision: rules), `composition_for(bp, override)` (explicit id | `pinned` | default `comp_mock_s2s_v1`), credential resolution per s2s adapter (env→admin→ephemeral) with `credential_source` on the session; realtime-paced mock for `browser_voice`; filtered event mirror to client (`FORWARDED_EVENT_TYPES`: turn/interruption/latency/handoff — no transcripts/audio/secrets)
- `qevion/runtime/app.py` — `GET /api/compositions` (with `credential_source`), WS `?composition=`, `_run_guarded` (PermissionError → `error` `provider_credential_missing` + close; other → `provider_failure`), WS 4400 unknown composition; session detail exposes `interruptions[]` with t0..t4 + `t1_to_t3_ms`/`t1_to_t4_ms`
- `qevion/core/session.py` — interruption `latency.sample` carries `response_id` (metadata only; no business logic added to Core)
- `qevion/adapters/providers/mocks.py` — `realtime` mode: 100 ms chunk pacing, response streamed in a background task so barge-in lands mid-response (mirrors live provider behaviour); non-realtime path unchanged
- `web/src/audio/worklet.ts` + `client.ts` — AudioWorklet PCM16 24 kHz capture (20 ms Int16 frames) and ring-buffer playout (`flush`/`tick`); `VoiceClient(send, control, events)` emits `playout_started` on the first frame actually reaching the speaker, `stopPlayout()` on interruption; linear resample fallback
- `web/src/console.ts` — composition selector from `/api/compositions`, mic toggle + level meter, `browser_voice` channel when mic on, binary frames → `VoiceClient.enqueue`, `stop_playout` → `stopPlayout()`, error codes surfaced, **Interruption metrics (QV-INT)** card listing t0..t4, t1→t3, t1→t4, heard/unheard, forced; `web/dist` rebuilt (tsc strict 0 errors) and tracked
- `scripts/e2e_voice_probe.py` — reusable E2E probe over any base URL (WSS voice session → barge-in → watermarks → REST interruptions → verdict)
- `config/locale_packs/*`, `config/voice_profiles/*` (3 profiles with provider_voice_map mock/openai_realtime) — already in tree; consumed by the registry

## Verified work (command → result)
- `scripts/ci_local.sh` → `CI_LOCAL: PASS` (ruff, ruff format, mypy strict, lint-imports 5 kept / 0 broken, schema drift 41 ok, pytest **219 passed / 0 failed**, verify.sh PASS incl. secret scan, license gate 0 denied) — `evidence/P4/CP-0007/junit.xml`
- `pytest tests/test_runtime_api.py tests/test_adapter_openai_realtime.py tests/test_adapters_turn.py` → 38 passed incl. `test_ws_voice_binary_roundtrip_barge_in_records_t0_t4` (binary PCM in/out over WS, `stop_playout`, `latency.sample` segment=interruption with t0≤t1≤t2≤t3≤t4, not forced, REST `interruptions[]`)
- `cd web && npm run build` → tsc strict 0 errors, 11 modules, dist committed (`index-CdcOa2cA.js`)
- `uvicorn qevion.main:app` on :8000 → `/api/health` ok; `/api/compositions` → `comp_mock_s2s_v1 (default, none)`, `comp_s2s_openai_v1 (env)`; public URL `https://8000-iwvtoaa5bc44eo8z5nmoz-2e1b9533.sandbox.novita.ai`; Playwright `#/console` loads with 0 console errors
- `scripts/e2e_voice_probe.py <preview>` ×10 over public WSS → 10/10 PASS — `evidence/P4/CP-0007/latency_table.json`: **QV-ACC-017** first audio frame p50 = 5 ms (< 1500 ms) · **QV-ACC-018** all 5 timestamps recorded in every run, server t1→t3 p95 = 2 ms, client wall barge→stop_playout p95 = 11 ms (≤ 300 ms), forced = false
- `git ls-remote origin main` == local HEAD after every commit

## Unverified work
- none

## Files changed since cp/CP-0006
```
 QEVION_WORK_STATE.md                               |   4 +-
 docs/registry/traceability.yaml                    |   6 +-
 evidence/P4/CP-0007/e2e_voice_preview.json         |  84 +++++
 evidence/P4/CP-0007/index.json                     |  49 +++
 evidence/P4/CP-0007/junit.xml                      |   1 +
 evidence/P4/CP-0007/latency_table.json             | 214 +++++++++++++
 evidence/P4/CP-0007/licenses.json                  | 252 +++++++++++++++
 evidence/ci/junit.xml                              |   2 +-
 qevion/adapters/providers/mocks.py                 |  25 +-
 qevion/adapters/providers/openai_realtime.py       | 349 +++++++++++++++++++++
 qevion/adapters/turn/silero.py                     | 144 +++++++++
 qevion/adapters/turn/smart_turn.py                 | 155 +++++++++
 qevion/core/session.py                             |   1 +
 qevion/runtime/app.py                              |  79 ++++-
 qevion/runtime/store.py                            | 115 ++++++-
 recovery/snapshots/CP-0006.manifest.json           | 221 +++++++++++++
 scripts/e2e_voice_probe.py                         |  50 +++
 tests/test_adapter_openai_realtime.py              | 233 ++++++++++++++
 tests/test_adapters_turn.py                        | 162 ++++++++++
 tests/test_runtime_api.py                          | 147 +++++++++
 .../{index-CfRP36Fx.css => index-BCGgzEuf.css}     |   2 +-
 web/dist/assets/index-CdcOa2cA.js                  |  69 ++++
 web/dist/assets/index-CehS7ygv.js                  |   2 -
 web/dist/index.html                                |   4 +-
 web/src/api.ts                                     |  23 +-
 web/src/audio/client.ts                            | 125 ++++++++
 web/src/audio/worklet.ts                           |  75 +++++
 web/src/console.ts                                 | 148 ++++++++-
 web/src/style.css                                  |   4 +
 29 files changed, 2697 insertions(+), 48 deletions(-)
```

## Tests / evidence
- `evidence/P4/CP-0007/{junit.xml,licenses.json,latency_table.json,e2e_voice_preview.json,index.json}`
- `docs/registry/traceability.yaml`: QV-ACC-017 accepted, QV-ACC-018 accepted, QV-ACC-019 in_progress (replay → P6)

## Known failures (classified)
- none

## Known risks
- ACC-017/018 numbers are on the **mock** provider (realtime-paced, loopback network); real-provider latencies unmeasured until an operator test key smoke (A5). The pipeline (AudioWorklet → WS → provider → cancel → stop_playout) is exercised end-to-end, the provider is not.
- `openai_realtime` adapter verified only against a scripted socket (protocol fixture), never a live endpoint — capabilities declared UNVERIFIED where appropriate.
- Silero runs in `silero_fallback_energy` mode unless the ONNX model is present (declared honestly in capabilities).
- Browser mic path (getUserMedia/AudioWorklet) type-checked and built but not exercised by Playwright (headless has no mic); the WS/binary contract it drives is covered by the probe.
- QV-ACC-019 (deterministic replay) deferred to P6 with the evaluation harness.
- Sandbox resets keep wiping `.venv`/`node_modules` (incidents #9–#15); everything of value is committed on `main`.

## Exact next step
- `tag_checkpoint.sh CP-0007` → `snapshot.sh CP-0007` → update `QEVION_WORK_STATE.md` (stage P4 COMPLETE → P5) → P5 step 1: `qevion/simulation/` scenario runner (drives `core.session.Session` with scripted caller turns from `simulation_cases` on the mock composition; produces a SimulationReport contract) + Activation gate (preflight strict + simulation pass → READY) + Outbound stub → CP-0008.
