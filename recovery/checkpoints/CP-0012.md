# CP-0012 — WS keepalive/heartbeat lifecycle fix + header/status mismatch; real-browser voice smoke on OpenAI

| Field | Value |
|---|---|
| Checkpoint ID | CP-0012 |
| Timestamp (UTC) | 2026-09-23T17:49:56Z |
| Phase | P6+ |
| Git SHA (state described) | ef595dd63b294766bdcb7467b78ddd80f5919609 |
| Status | VERIFIED |

## Completed work
- **WS keepalive lifecycle (field blocker `1011 keepalive ping timeout` on browser_voice + OpenAI):** `runtime/app.py` — `WsTransport` inbound queue bounded (`INBOUND_QUEUE_MAX`=400; oldest audio dropped on overflow, control messages never), pump only decodes+enqueues so uvicorn's paused-read/Pong path never stalls; server application heartbeat `ping`/`pong` every 10 s with stale-close at 45 s; close code/reason + heartbeat stats recorded on `LiveSession` and exposed via `/api/sessions`; `ready` carries actual session facts (composition_id, channel, s2s provider/model, credential sources). `qevion/main.py` — `python -m qevion.main` entrypoint with `UVICORN_WS_KWARGS` (ping 25 s, timeout 60 s, max_queue 256; env-overridable). Contracts: `ServerMessageType.PING`, `ClientMessageType.PONG` (+ regenerated JSON Schemas). Provider architecture and the OpenAI Realtime GA adapter untouched.
- **Operator Console header/status mismatch:** header pill shows the runtime default only while idle; on `ready` it switches to `live · <actual composition> · <channel> · <provider>`; status pill/chat line echo the same facts; console answers heartbeat pings; Sessions table adds composition, channel, close code, heartbeat columns. `web/dist` rebuilt.
- **Tests/scripts:** `tests/test_ws_keepalive.py` (real uvicorn, 1 s ping / 1 s timeout window, realtime browser_voice soak 8 s + 22 s, bounded-queue unit); `scripts/ws_keepalive_soak.py`; `scripts/browser_voice_smoke.py` (Playwright Chromium, fake mic, scripted utterances, WS shim capturing close code + pings/pongs). README run instructions.

## Verified work (command → result)
- `scripts/ci_local.sh` → **CI_LOCAL: PASS** — 273 tests, 0 failures (ruff, format, mypy strict, lint-imports 5/5, schema drift, verify.sh, license gate). `evidence/ci/junit.xml`.
- `pytest tests/test_ws_keepalive.py` → 3 passed: with a 1 s protocol ping window a realtime browser_voice session closes 1000 (never 1011), `ready.composition_id == comp_mock_s2s_v1`, speaking→LISTENING observed, ≥2 server heartbeats answered.
- `OPENAI_API_KEY=… scripts/browser_voice_smoke.py evidence/live/browser_voice_smoke_openai.json --base https://8000-…sandbox.novita.ai --composition comp_s2s_openai_v1 --seconds 75 --inject-key-env OPENAI_API_KEY` → **10/10 checks passed** (`evidence/live/browser_voice_smoke_openai.json`): mic on → connect → `session ready — composition comp_s2s_openai_v1 · browser_voice · s2s openai_realtime:gpt-realtime · credential ephemeral_ui` → header `live · comp_s2s_openai_v1 · browser_voice · openai_realtime:gpt-realtime` → speaking (63 binary frames, audio_start/end ×2) → listening → second spoken user turn answered → 7 heartbeats / 7 pongs (RTT 3.5 ms, 0 dropped frames) → no close during the 75 s soak, **no 1011** → Hang up → close 1000 `user_bye`; header reverts to `ok · default comp_mock_s2s_v1 …`. Total session 86 s.
- Same smoke on `comp_mock_s2s_v1` (localhost) → 10/10.

## Unverified work
- Manual operator re-test in their own browser (the automated smoke uses Chromium fake mic + scripted utterances). Real-provider barge-in timings (ACC-017/018) still backlog.

## Files changed since cp/CP-0011
```
 QEVION_WORK_STATE.md                               |  11 +-
 README.md                                          |  17 +-
 evidence/ci/junit.xml                              |   2 +-
 evidence/ci/licenses.json                          |  15 ++
 evidence/live/browser_voice_smoke_openai.json      | 101 +++++++
 .../schemas/transport_client_v1.schema.json        |   3 +-
 .../schemas/transport_server_v1.schema.json        |   3 +-
 qevion/contracts/transport.py                      |   2 +
 qevion/main.py                                     |  29 +-
 qevion/runtime/app.py                              | 130 ++++++++-
 qevion/runtime/store.py                            |   3 +
 recovery/snapshots/CP-0011.manifest.json           | 291 +++++++++++++++++++++
 scripts/browser_voice_smoke.py                     | 272 +++++++++++++++++++
 scripts/ws_keepalive_soak.py                       | 191 ++++++++++++++
 tests/test_ws_keepalive.py                         | 162 ++++++++++++
 web/dist/assets/index-AAGCoTjJ.js                  |  69 +++++
 web/dist/assets/index-DXLo1lE6.js                  |  69 -----
 web/dist/index.html                                |   2 +-
 web/src/console.ts                                 |  44 +++-
 web/src/main.ts                                    |  39 ++-
 20 files changed, 1352 insertions(+), 103 deletions(-)
```

## Tests / evidence
- (paths)

## Known failures (classified)
- none

## Known risks
- (fill)

## Exact next step
- (one action)
