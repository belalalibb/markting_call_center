# CP-0011 — OpenAI Realtime GA live S2S verification + live-found fixes (QV-ACC-022 accepted)

| Field | Value |
|---|---|
| Checkpoint ID | CP-0011 |
| Timestamp (UTC) | 2026-09-23T14:26:41Z |
| Phase | P6+ |
| Git SHA (state described) | 73a8de5cbae6f50642ec4dfcb24d26c9ad0844d7 |
| Status | VERIFIED |

## Completed work
- **OpenAI Realtime adapter → GA wire protocol** (`qevion/adapters/providers/openai_realtime.py`): `session.update` now sends `type: realtime`, `output_modalities: [audio]`, nested `audio.input {format {type: audio/pcm, rate: 24000}, transcription, turn_detection}` / `audio.output {format, voice}`; `OpenAI-Beta` header removed. Beta shape was rejected live ("The Realtime Beta API is no longer supported"). Offline tests updated to the GA shape.
- **Live-found defects fixed (each with a regression test):** `admin/credentials.py` — providers sharing an env var (`openai` ↔ `openai_realtime`) are aliases for admin-store/ephemeral lookups; `core/dialog_machine.py` — `LISTENING` accepts `response_started` / `tool_requested` (real providers speak again right after a tool result without a new user turn; previously `failure.classified illegal_dialog_transition` on every tool follow-up); `runtime/store.py` — core `Session` receives the registry `session_id` (wire id == `/api/sessions/{id}`); `outcome.produced` + `session.ended` mirrored to the client.
- **Live scripts:** `scripts/openai_realtime_live_probe.py` (raw adapter), `scripts/openai_realtime_live_ws.py` (server E2E via Admin ephemeral key). Key never leaves env/memory; evidence carries a fingerprint only.
- **Registry/docs:** QV-ACC-022 → **accepted** (S2S = OpenAI Realtime GA, decision = TypeSafe Jev). Work state incidents #17 (resets #30–#31, pgrep self-kill, sandbox proxy key) and #18 (4 mock-invisible defects).

## Verified work (command → result)
- `scripts/ci_local.sh` → **CI_LOCAL: PASS** — 270 tests, 0 failures (ruff, format, mypy strict, lint-imports 5/5 KEPT, schema drift, verify.sh secret scan, license gate).
- `OPENAI_API_KEY=… scripts/openai_realtime_live_probe.py evidence/live/openai_realtime_live_probe.json` → passed: open 238 ms, `session.created` +12 ms, text → `response.created` +60 ms, first audio +337 ms, `response.done` at 2.0 s with 5.95 s pcm16 audio + 52 transcript chars, 0 errors.
- `OPENAI_API_KEY=… scripts/openai_realtime_live_ws.py evidence/live/openai_realtime_live_ws_e2e.json` against `uvicorn qevion.main:app` (key injected via `POST /api/admin/test-key provider=openai`) → **passed**: session detail `composition_id comp_s2s_openai_v1`, `credential_source ephemeral_ui`, 3 ar-EG text turns → 88 binary audio frames (1,077,600 B = 22.45 s), `record_field` executed twice with `validate_field` RULE decisions (`q1_overall`, `consent`), `turn.ended` ×3, 0 provider errors, 0 `failure.classified`, TTFA 453 / 968 / 965 ms; `bye` → `outcome.produced abandoned` (expected: survey not completed in 3 turns) → `session.ended user_bye`.
- `grep -rl sk-proj evidence/ scripts/` → no secret in tree.

## Unverified work
- Gemini Live adapter path (no key). Real-provider barge-in timings (ACC-017/018 still mock-measured). Browser mic path against OpenAI (headless has no mic).

## Files changed since cp/CP-0010
```
 QEVION_WORK_STATE.md                           |  15 +-
 docs/registry/traceability.yaml                |   2 +-
 evidence/ci/junit.xml                          |   2 +-
 evidence/live/openai_realtime_live_probe.json  |  52 +++++
 evidence/live/openai_realtime_live_ws_e2e.json | 145 +++++++++++++
 qevion/adapters/providers/openai_realtime.py   |  39 ++--
 qevion/admin/credentials.py                    |  24 ++-
 qevion/core/dialog_machine.py                  |  12 +-
 qevion/runtime/store.py                        |   7 +-
 recovery/snapshots/CP-0010.manifest.json       | 285 +++++++++++++++++++++++++
 scripts/openai_realtime_live_probe.py          | 142 ++++++++++++
 scripts/openai_realtime_live_ws.py             | 193 +++++++++++++++++
 tests/test_adapter_openai_realtime.py          |   6 +-
 tests/test_adapters_mocks.py                   |  12 ++
 tests/test_core_primitives.py                  |  17 ++
 tests/test_runtime_api.py                      |   1 +
 16 files changed, 922 insertions(+), 32 deletions(-)
```

## Tests / evidence
- (paths)

## Known failures (classified)
- none

## Known risks
- (fill)

## Exact next step
- (one action)
